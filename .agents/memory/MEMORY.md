- [Yahoo Finance Screener ETF API](yahoo-screener-etf.md) — field formats (changePct/ER/returns are NOT fractions), auth pattern, 5,649 ETF scale, 10-min cache approach
- [Sidekick UI architecture](sidekick-ui.md) — React Context provider pattern for global AI panel state, with `useSidekick()` hook exposing `openWithPrompt()`, `openWithSymbol()`, `prefill`, and `inputRef` for external triggers.
- [TradingView-style chart context menu](chart-context-menu.md) — Use `onContextMenu` on a wrapper div around the chart canvas (not on the canvas itself, which is a foreign object) to intercept right-clicks and render a fixed-position React overlay.
- [Watchlist AI integration](watchlist-ai-icon.md) — Subtle Sparkles icon on row hover (opacity 0 → 100 on group hover) prevents UI clutter while keeping AI access one click away. Stop event propagation to prevent row navigation.
- [Keyboard shortcuts for panels](keyboard-shortcuts.md) — Global `document.addEventListener('keydown')` with `e.metaKey || e.ctrlKey` for cross-platform Cmd+K/Ctrl+K. Use `e.preventDefault()` to override browser defaults. Escape closes the panel.
- [Collapsed sidebar UX pattern](collapsed-sidebar.md) — 44px fixed vertical bar with icon at top, rotated label in middle, and status dot at bottom. Hover state transitions from dark to slightly lighter. Opens to 360px right panel with backdrop on mobile.
